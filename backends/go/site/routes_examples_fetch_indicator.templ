package site

import "time"

templ fetchIndicatorEmpty() {
	<div id="greeting">No data yet, please wait</div>
}

templ fetchIndicatorGreeting() {
	<div id="greeting">Hello, the time is { time.Now().Format(time.RFC3339) }</div>
}
